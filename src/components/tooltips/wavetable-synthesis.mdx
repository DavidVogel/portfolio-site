import React from 'react';
import useBaseUrl from '@docusaurus/useBaseUrl';
import styles from '@site/src/components/tooltip.module.scss';

export default function WavetableSynthesis() {
  const referencePageUrl = useBaseUrl('docs/Synthesis-Reference/Synthesis-Reference#wavetable-synthesis');
  return (
    <>
      <h3>Wavetable Synthesis</h3>
      <p>
        <strong>Wavetable Synthesis</strong> is a method of sound synthesis that uses a collection of single-cycle waveforms to create complex timbres.
      </p>
      <p className={styles.popupRefLink}><a href={referencePageUrl}>Go to reference page...</a></p>
    </>
  );
}
